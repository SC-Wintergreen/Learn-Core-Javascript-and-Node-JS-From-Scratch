let str1 = "Volgograd";
// Find the index in which o appears for the first time

console.log(str1.indexOf("o"));

// Find the index in which o appears again after 1st index
console.log(str1.indexOf("o", 2));

// Find the first occurance of g in Volgograd
console.log(str1.indexOf("g"));

// Find the index in which g appears in volgograd after 3rd index
console.log(str1.indexOf("g", 4));

// indexOf For Substrings

let b = "Siberia";
let c = "ber";
console.log(b.indexOf(c));

// Volodymyr

// Vladimir
